# o livro NÃO vai pro git: o repo é público e serve o GitHub Pages —
# commitar isso publicaria os 270 páginas do livro inteiro pra qualquer um
book-1.pdf
*.pdf

__pycache__/
*.pyc
.DS_Store
teste-tom3/

# Claude Code: o launch.json VAI pro git (é a config do servidor de preview, igual pros
# três e sem segredo nenhum). O resto é de cada máquina — permissões concedidas, estado
# de sessão — e não tem por que viajar no repositório.
.claude/*
!.claude/launch.json
